<Window x:Class="StretchReminderApp.UI.StretchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Time to Stretch!" Height="600" Width="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Let's do some stretching!" 
                   FontSize="24" Margin="20,20,20,10" HorizontalAlignment="Center"/>

        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="20">
            <Image x:Name="WebcamPreview" Stretch="Uniform"/>
        </Border>

        <ProgressBar Grid.Row="2" x:Name="ProgressBar" Height="20" Margin="20,5,20,10"
                     Minimum="0" Maximum="100" Value="0"/>

        <Grid Grid.Row="3" Margin="20,0,20,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" x:Name="StatusTextBlock" 
                       VerticalAlignment="Center" Text="Waiting for camera..."/>

            <Button Grid.Column="1" x:Name="SkipButton" Content="Skip for today" 
                    Padding="15,8" Click="SkipButton_Click"/>
        </Grid>
    </Grid>
</Window>